{
  "name": "counter-rust",
  "version": "1.0.0",
  "license": "(MIT AND Apache-2.0)",
  "scripts": {
    "deploy": "cd contract && sh deploy.sh",
    "build": "npm run build:contract",
    "build:contract": "cd contract && sh build.sh",
    "test": "npm run build:contract && npm run test:unit",
    "test:unit": "cd contract && cargo test"
  },
  "devDependencies": {
    "near-cli": "^3.3.0",
    "@types/fs-extra": "^11.0.1",
    "bn.js": "^5.2.1",
    "dotenv": "16.0.3",
    "fs-extra": "^11.1.1",
    "near-api-js": "^2.1.3",
    "ts-node": "^10.9.1",
    "typescript": "5.0.4",
    "@types/bn.js": "5.1.1"
  },
  "dependencies": {}
}
